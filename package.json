{
  "name": "parcel-static",
  "version": "1.0.0",
  "licence": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url ./",
    "deploy": "parcel build index.html --public-url ./ & copyfiles --flat views/*.html dist/views"
  },
  "devDependencies": {
    "node-sass": "^4.9.0",
    "stylus": "^0.54.5"
  },
  "dependencies": {
    "flickity": "^2.1.1",
    "parcel-bundler": "^1.8.1"
  }
}
